import React from 'react';

function page() {
  return (
    <div className="h-[50vh] bg-blue-400">
      home page pm2 logs flushed github webhooks api updated server just runs pm2 stop then ignores
      other chained commands
    </div>
  );
}

export default page;
