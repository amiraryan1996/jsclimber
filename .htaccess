
RewriteEngine On

# Exclude .well-known directory if using Let's Encrypt or other ACME validation
RewriteCond %{REQUEST_URI} !^/\.well-known

# Proxy /demo path to the Node.js application running on port 8364
RewriteCond %{REQUEST_URI} ^/demo
RewriteRule ^(.*)$ http://136.243.36.95:8364/$1 [P,L]
